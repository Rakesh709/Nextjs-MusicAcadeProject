/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com','api.microlink.io'], // Add Unsplash domain here // Microlink Image Preview
    },
  };
  
  export default nextConfig;
  